<%= form_for (@need) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_field :needing, placeholder: "Compose need..." %>
  </div>
  <%= f.submit "Create Need", class: "btn btn-large btn-primary" %>
<% end %>

