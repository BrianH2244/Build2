<%= form_for (@project) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_field :goal, placeholder: "Compose new project..." %>
  </div>
  <%= f.submit "Create Project", class: "btn btn-large btn-primary" %>
<% end %>
