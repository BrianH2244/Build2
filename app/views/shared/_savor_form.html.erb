<%= form_for (@savor) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_field :savoring, placeholder: "Compose savored moment..." %>
  </div>
  <%= f.submit "Create Savor", class: "btn btn-large btn-primary" %>
<% end %>

