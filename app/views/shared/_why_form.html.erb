<%= form_for (@question) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_field :why, placeholder: "Compose vision statement..." %>
  </div>
  <%= f.submit "Create Question", class: "btn btn-large btn-primary" %>
<% end %>
