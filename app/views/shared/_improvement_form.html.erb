<%= form_for (@improvement) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_field :improving, placeholder: "Compose improved skill..." %>
  </div>
  <%= f.submit "Create Improvement", class: "btn btn-large btn-primary" %>
<% end %>
