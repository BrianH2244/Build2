<% provide(:title, @user.name) %>
<div class="row">
  <aside class="span4">
    <section>
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1>
    </section>
  </aside>

<div class="clear"></div>

<div class="shine">
  <% unless @user.savors.empty? %>
    <h3><%= link_to 'Savored', reflect_path %></h3>

    <table>
      <%= render @savors %>
    </table>

     <p><%= will_paginate @savors %></p>
   <% end %>
</div>

<div class="shine">
  <% unless @user.habits.empty? %>
    <h3><%= link_to 'Habits', reflect_path %></h3>

    <table>
      <%= render @habits %>
    </table>

     <%= will_paginate @habits %>
   <% end %>
</div>

<div class="shine">
  <% unless @user.improvements.empty? %>
    <h3><%= link_to 'Improvement', reflect_path %></h3>

    <table>
      <%= render @improvements %>
    </table>

     <%= will_paginate @improvements %>
   <% end %>
</div>

<div class="shine">
  <% unless @user.needs.empty? %>
    <h3><%= link_to 'Needs', reflect_path %></h3>

    <table>
      <%= render @needs %>
    </table>

     <%= will_paginate @needs %>
   <% end %>
</div>

<div class="clear"></div>

<div class="left">
  <% unless @user.projects.empty? %>
    <h3><%= link_to 'Current Projects', projects_path %></h3>

    <table>
      <%= render @projects %>
    </table>

    <%= will_paginate @projects %>
  <% end %>
</div>

<div class="clear"></div>

<div class="left">
  <% unless @user.weeks.empty? %>
    <h3><%= link_to 'Weekly MITs', weekly_path %></h3>

    <table>
      <%= render @weeks %>
    </table>

    <%= will_paginate @weeks %>
  <% end %>
</div>

<div class="clear"></div>

<div class="left">
  <% unless @user.quotes.empty? %>
    <h3><%= link_to 'Inspiring Quotations', quotes_path %></h3>

    <table>
      <%= render @quotes %>
    </table>

    <%= will_paginate @quotes %>
  <% end %>
</div>

<div class="clear"></div>
